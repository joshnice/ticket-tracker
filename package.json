{
	"name": "ticket-tracker",
	"version": "1.0.0",
	"description": "Tweets the amount of tickets left for each game for Derby",
	"main": "index.js",
	"workspaces": ["./aws", "./puppeteer", "./twitter"],
	"scripts": {
		"aws-deploy": "cd aws && cdk deploy",
		"aws-destroy": "cd aws && cdk destroy",
		"dev": "node ./puppeteer/local.js"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.6.3"
	},
	"author": "joshnice",
	"license": "ISC"
}
